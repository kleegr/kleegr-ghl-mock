# Prompt templates (placeholder)

Per the product plan (§10, §19), fake data, conversations, and tutorial scripts are
intended to be regenerated from editable prompt templates so non-engineers can expand
content without code.

V1 keeps generation as code config: see `src/data/seed.ts` for the deterministic data
generator. Prompt-template files will live here in a later task.
