import { PageHeader } from '@/components/ui/primitives';

export function Settings() {
  return (
    <div>
      <PageHeader title="Settings" subtitle="Account settings — Wave 1 stub" />
      <div className="px-5 py-8 text-sm text-ink-muted">Settings module coming in a future wave.</div>
    </div>
  );
}
