/**
 * Public entry point for the help model.
 *
 * Lets future code import from `@/help` instead of reaching into the concrete
 * file, e.g. `import { getHelp, HELP_CONTENT } from '@/help'`. Add the help
 * popover/panel (Developer 2) and any help utilities against this barrel.
 */
export * from './helpContent';
